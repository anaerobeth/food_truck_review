<%= simple_form_for @food_truck do |f| %>
  <%= f.input :name %>
  <%= f.file_field :photo  %>
  <%= f.input :description %>

  <%= f.button :submit %>
<% end %>

<%= link_to 'Cancel', food_trucks_path %>
