<%= simple_form_for([@truck, @review]) do |f| %>
  <%= f.input :body %>
  <%= f.collection_radio_buttons :is_good, [[true, 'Good'], [false, 'Bad']], :first, :last %>
  <%= f.button :submit %>
<% end %>



